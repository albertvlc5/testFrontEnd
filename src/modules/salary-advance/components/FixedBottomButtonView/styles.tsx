import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonBox: {
    flex: 1,
    alignSelf: 'flex-end',
    alignItems: 'center',
    justifyContent: 'center',
  },
  stickToBottom: {
    position: 'absolute',
    bottom: 0,
    height: 90,
    right: 30,
  },
});
