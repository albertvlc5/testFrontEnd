import environment from './environment';
import i18n from './i18n';
import { AtWorkNavigationStack, AtWorkRoutes } from './routes';

export const AtWorkModule = {
  navigationStack: AtWorkNavigationStack,
  routes: AtWorkRoutes,
  i18n,
  environment,
};
